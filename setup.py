from setuptools import setup
from setuptools import find_packages

setup(name='sparsam',
      version='0.1',
      author='Maximilian Nielsen',
      author_email='m.nielsen@uke.de',
      packages=find_packages())
